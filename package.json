{
  "name": "activity-sampling",
  "private": true,
  "engines": {
    "node": ">=18.7.0"
  },
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "npm start --workspace --if-present",
    "test": "npx jest",
    "build": "npm run build --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "8.2.2",
    "eslint": "9.0.0",
    "jest": "29.7.0",
    "jest-environment-jsdom": "29.7.0",
    "prettier": "3.2.5"
  }
}
